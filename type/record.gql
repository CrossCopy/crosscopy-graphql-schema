type Record {
  inUserId: Int!
  userId: String!
  value: String!
  type: String!
  profile: String!
  createdAt: String!
  device: String!
}

type Query {
  records: [Record]
  recordsLaterThanId(id: Int!): [Record]
}
