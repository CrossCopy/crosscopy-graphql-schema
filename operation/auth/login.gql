mutation login($email: String!, $password: String!) {
  login(email: $email, password: $password) {
    accessToken
    user {
      email
      _id
      username
      profiles
      records {
        inUserId
        value
        type
        profile
        createdAt
        device
      }
    }
    success
  }
}
